#include "minishell.h"

int	exec_cd(char **com, char **envp)
{
	// size_t	row;
	// char	*path;
	// char	*pwd;
	(void)envp;
	(void)com;
	// (void)pwd;
	// row = 1;
	// path = NULL;
	// path = getcwd(NULL, 0);
	// chdir("/Users/takumasaokamoto/Desktop");
	// printf("%s\n", path);
	// if (!com[row])
	// {
	// 	// pwd = getcwd(NULL, 0);
	// 	// path = getenv("HOME");
	// 	chdir(path);
	// }
	return (SUCCESS);
}
